// This component is no longer needed in Next.js
// SEO is handled via metadata exports in page files
// Keeping this as a no-op for backward compatibility
import React from "react";

function SeoHeader({ title, description, canonical }) {
  // In Next.js, SEO is handled via metadata exports in page files
  // This component is kept for backward compatibility but does nothing
  return null;
}

export default SeoHeader;
